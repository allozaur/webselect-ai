{
  "name": "@webselect-ai/frontend",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "build:browser-extension": "turbo run build:browser-extension",
    "build:website": "turbo run build:website",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write .",
    "check-types": "turbo run check-types"
  },
  "devDependencies": {
    "prettier": "^3.5.0",
    "prettier-plugin-svelte": "^3.3.3",
    "turbo": "^2.4.4"
  },
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20"
  }
}